# PPPoEでインターネットに接続する(Ubuntu 18.0)

```
$ sudo apt-get install -y pppoe pppoeconf
$ sudo pppoeconf
```
Enter ID/PW. 

After reboot,
Setting >> Netwirk >> bluetooth >>> PPP(ppp0) ON >> Link negotiation (Automatic)
